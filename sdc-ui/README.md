# sdc-ui (Angular placeholder)
Este diretório é um placeholder para o app Angular (Node 20 + Angular CLI 18).

Passos sugeridos (fora do Maven):
npm install -g @angular/cli@18
ng new sdc-ui --routing --style=scss
# mover o conteúdo aqui ou manter como repo separado
